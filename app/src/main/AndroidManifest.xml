<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetoarea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultActivityTriangle"></activity>

        <activity android:name=".ResultActivityCircle"
            android:parentActivityName=".CircleShapeActivity"></activity>

        <activity android:name=".ResultActivitySquare"
            android:parentActivityName=".SquareShapeActivity"></activity>
        
        <activity android:name=".CircleShapeActivity" />
        <activity android:name=".TriangleShapeActivity" />
        <activity android:name=".SquareShapeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>